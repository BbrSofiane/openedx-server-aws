name: openedx-server
runtime: python
description: A Single instance Open edX Server.
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: eu-west-2
    myAccessToken:
      description: My access token
      secret: true
